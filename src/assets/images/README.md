# Image sources

These source images feed Astro's Image component and responsive build variants. Public copies remain for legacy CSS backgrounds, gallery links and social images. When replacing an existing image, update both copies; new content images should be added here and in public/assets/img when a public URL is needed. The built output is verified by npm run audit:images.
