---
import { legacyScriptAssets, shouldLoadScript } from '../../data/assets';

interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;
---

{legacyScriptAssets.filter((asset) => shouldLoadScript(asset, currentPath)).map((asset) => <script is:inline src={asset.src} defer></script>)}
